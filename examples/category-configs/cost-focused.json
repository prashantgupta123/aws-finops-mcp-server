{
  "mcpServers": {
    "aws-finops-cost": {
      "command": "python",
      "args": ["-m", "aws_finops_mcp"],
      "env": {
        "MCP_TOOL_CATEGORIES": "cost,cleanup,capacity"
      }
    }
  }
}
